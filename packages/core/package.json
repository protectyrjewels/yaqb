{
  "name": "@query-builder/core",
  "version": "1.0.0",
  "main": "src/index.ts",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "vitest run --bail 1",
    "test:coverage": "vitest run --coverage --coverage.exclude=examples --coverage.exclude=dist --coverage.exclude=types"
  },
  "author": "Adolfo Silva <a@adolfosilva.dev>",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "vitest": "^2.0.5"
  }
}
