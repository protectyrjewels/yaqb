{
  "name": "@yaqb/pg",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run --bail 1",
    "coverage": "vitest run --coverage --coverage.exclude=examples --coverage.exclude=dist --coverage.exclude=types --coverage.exclude=tests"
  },
  "author": "Adolfo Silva <a@adolfosilva.dev>",
  "license": "MIT",
  "dependencies": {
    "@scaleleap/pg-format": "^1.0.0"
  },
  "peerDependencies": {
    "@yaqb/core": "^1.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.0.5",
    "@yaqb/core": "^1.0.0",
    "vitest": "^2.0.5"
  }
}
