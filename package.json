{
  "name": "@yaqb",
  "version": "1.0.0",
  "description": "Yet Another Query Builder is an adaptable tool that assists developers in constructing SQL and MongoDB queries directly from structured rules. Moreover, it articulates the complex query logic into a human-readable natural language sentence, enhancing understandability and documentation.",
  "author": "Adolfo Silva <a@adolfosilva.dev>",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint packages",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=16.0.0"
  },
  "workspaces": [
    "packages/core",
    "packages/mongo",
    "packages/pg",
    "packages/ui-ng"
  ],
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "@types/eslint__js": "^8.42.3",
    "eslint": "^9.9.0",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.0.1"
  }
}
