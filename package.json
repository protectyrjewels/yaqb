{
  "name": "query-builder",
  "version": "1.0.0",
  "description": "Query Builder is an adaptable tool that assists developers in constructing SQL and MongoDB queries directly from structured rules. Moreover, it articulates the complex query logic into a human-readable natural language sentence, enhancing understandability and documentation.",
  "author": "Adolfo Silva <a@adolfosilva.dev>",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "lint": "eslint packages",
    "test": "npm run test --workspaces --if-present"
  },
  "workspaces": [
    "packages/query-builder-core",
    "packages/query-builder-mongo",
    "packages/query-builder-pg",
    "packages/query-builder-ui-ng"
  ],
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "@types/eslint__js": "^8.42.3",
    "eslint": "^9.9.0",
    "typescript": "^5.5.4",
    "typescript-eslint": "^8.0.1"
  }
}
